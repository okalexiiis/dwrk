# dwrk 
A Project Management CLI Tool 

## To Do

- [ ] By Default, the Clone command should search a project with the same name a clone it directly. If a project is not found, it should ask whether to create to create the directory
- [ ] Add a flag to the open command to change the current directory to the project directory.
  ```bash
  dwrk open -t
  dwrk open --terminal
  ```
